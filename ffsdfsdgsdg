-- 🧱 Nút nhỏ mở/đóng menu
local ToggleButton = Instance.new("ImageButton")
ToggleButton.Parent = ScreenGui
ToggleButton.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
ToggleButton.BorderSizePixel = 0
ToggleButton.Position = UDim2.new(0.05, 0, 0.3, 0)
ToggleButton.Size = UDim2.new(0, 50, 0, 50)
ToggleButton.Image = "rbxassetid://90677395878536"
ToggleButton.Draggable = true

local UICorner2 = Instance.new("UICorner")
UICorner2.CornerRadius = UDim.new(1, 10)
UICorner2.Parent = ToggleButton

-- Biến lưu trạng thái menu
local isOpen = true
local FluentUI = Window -- Menu Fluent của bạn

-- 🖱️ Khi bấm nút → mở/đóng menu
ToggleButton.MouseButton1Click:Connect(function()
	isOpen = not isOpen
	if isOpen then
		FluentUI:SelectTab(Tabs.Main0) -- Mở lại giao diện
		FluentUI:Minimize(false)       -- Gọi mở menu
	else
		FluentUI:Minimize(true)        -- Gọi đóng menu
	end
end)
